<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.thundersharp.bombaydine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BombayDine"
        tools:replace="android:theme">
        <activity android:name=".user.ui.scanner.QrScanner"></activity>
        <activity android:name=".user.ui.menu.AllItemsActivity" />
        <activity android:name=".user.ui.orders.RecentOrders" />
        <activity android:name=".user.ui.home.MainPage" />
        <activity android:name=".user.ui.login.EmailAccountCreate" />
        <activity android:name=".user.ui.login.EmailLoginActivity" />
        <activity android:name=".user.ui.login.OtpVerificationActivity" />
        <activity android:name=".user.ui.login.LoginActivity" />
        <activity android:name=".user.ui.startup.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>